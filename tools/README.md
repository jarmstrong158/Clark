# tools/

Diagnostic and one-off scripts (value-head probes, checkpoint
salvage, plot generators). Run from the repo root so the
`clark/data/...` relative paths resolve, e.g. `python tools/probe_v3.py`.
Not part of the package or the training path.
